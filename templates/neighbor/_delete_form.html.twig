<form method="post" action="{{ path('app_neighbor_delete', {'id': neighbor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ neighbor.id) }}">
    <button class="btn">Delete</button>
</form>
