<form method="post" action="{{ path('app_community_delete', {'id': community.id}) }}" onsubmit="return confirm('¿Estás seguro de que deseas eliminar esta comunidad?');" style="display:inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ community.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>

