{% extends 'base.html.twig' %}

{% block title %}Opinions{% endblock %}

{% block body %}
    <h1>Opinions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opinion.id }}</td>
            </tr>
            <tr>
                <th>NameNeighbor</th>
                <td>{{ opinion.nameNeighbor }}</td>
            </tr>
            <tr>
                <th>DateOpinion</th>
                <td>{{ opinion.dateOpinion ? opinion.dateOpinion|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ opinion.body }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_opinions_index') }}">back to list</a>

    <a href="{{ path('app_opinions_edit', {'id': opinion.id}) }}">edit</a>

    {{ include('opinions/_delete_form.html.twig') }}
{% endblock %}
